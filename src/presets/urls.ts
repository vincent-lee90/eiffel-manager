export default class ManageUrls {
    static teamRewards = `manage/teamReward`
    static setHashrate = `manage/setHashrate`
    static gameLogs = `manage/game-logs`
    static statistics = `manage/statistics`
    static relation = `manage/memberInfo`
    static team = `manage/team`
    static login = `manage/login`
    static mintList=`manage/getMintList`
    static manualMint=`manage/manualMint`
}